# Separate Base Asset extraction from Derived Asset analysis

Script asset extraction creates only evidence-grounded Base Assets through one full-context extraction pass and one completeness-review pass. The Production Agent exclusively creates Derived Assets from reusable visual-state changes; confirmed re-extraction stages and validates the complete new result before atomically replacing selected Script relationships, preserving shared Assets and removing newly orphaned Assets with their dependent records.
